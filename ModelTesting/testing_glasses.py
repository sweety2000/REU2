import cv2
from matplotlib import pyplot as plt

#RECEIVE INPUT FROM GLASSES
test_image = cv2.imread(r#IMAGE PATH
test_array = cv2.reshape(#-dimensions, IMG SIZE, IMG SIZE, #dimensions)

CATEGORIES = #ITEMS

predict = model.predict(test_array) #this is prediction


#then we return predict to user and save it to shopping lists
